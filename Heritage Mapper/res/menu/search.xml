<menu xmlns:android="http://schemas.android.com/apk/res/android" >


    <!-- Wishlist, appear as action button -->
    <item android:id="@+id/action_wishlist"
          android:icon="@drawable/ic_action_view_as_list"
          android:title="@string/action_wishlist"
          android:showAsAction="ifRoom" />
    
     <!-- Visited list, appear as action button -->
    <item android:id="@+id/action_visited_list"
		  android:icon="@drawable/ic_visited"
          android:title="@string/action_visited_list"
          android:showAsAction="ifRoom" />

</menu>
